Installing git
    Select components
        Check all the options but the last one (check daily for git for windows updates)

    Chose the default editor user by Git
        Select visual studio code as Git's default editor

    Adjusting your PATH environment 
        Select the last option (Use Git and optional Unix tools from the Command Prompt)
    
    Choosing HTTPS transport backend 
        select the first option (Use the OpenSSL library)
    
    Configuring the line ending conversions
        select the first option (Checkout Windows-style, commit Unix-style line endings)

    Configuring the terminal emulator to use with Git Bash 
        select the selecton option (Use Windows's default console window)
    
    Configuring extra options
        select all the options 

tessting git version / in CMD (Command)
    git --version

Configuring git
    git config --global user.name "Baktash Sana"
    git config --global user.email "baktash11@tmail.com"

    
Installing Nodejs 
INstalling Node PackageManager (terminal VS-Code)
    npm install lite-server -g 
    npm install yarn -g 
    npm lite-server


Adding more meaning with HTML5
    ...
    Semantic elements in HTML5 such as <head>, <nav>, <section>, <article>, and <footer> 
    Each of these new elements has special meaning and rules for its use, where the previously used <div> has very little or no 
    semantic meaning.
    ...


Adding package.json
    yarn init 
    the question must be answered 
        name: 
        version:
        escription:
        entry point (index.js):
        repository url...:
        author:
        private: 

Adding query 
    yarn add jquery

Adding popper.js 
    yarn add popper.js

Adding bootstrap
    yarn add bootstrap

to install PackageManager
    yarn install 


JavaScripts
Document Object Mode (DOM)

In this course we will not use var to declear a variable
var allows the Variables to be hoisted.
HOISTED:
    In some propgraming languages the move the variable while compiling to to the top.

We wil use let, and it will prevent from hoisting 





Assets 
fontawesome.com
fortawesom
yarn add @fortawesome/fontawesome-free


